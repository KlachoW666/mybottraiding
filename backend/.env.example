# OKX API (Торговля)
# ВАЖНО: Не коммитьте .env в git! После утечки ключей — перевыпустите их на OKX.
OKX_API_KEY=
OKX_SECRET=
OKX_PASSPHRASE=

# OKX Testnet (демо-счёт): OKX_SANDBOX=1 — торговля на тестовом счёте
# OKX_SANDBOX=0

# Авто-трейдинг: исполнение ордеров через OKX. Без флага — только сигналы.
# Включить: AUTO_TRADING_EXECUTION_ENABLED=1 (осторожно: реальные деньги при OKX_SANDBOX=0)
# AUTO_TRADING_EXECUTION_ENABLED=0

# Прокси для запросов к OKX (обход Cloudflare). Список через запятую.
# PROXY_LIST=http://user:pass@ip:port,http://user:pass@ip2:port
PROXY_LIST=

PORT=3000
NODE_ENV=development
# LOG_LEVEL=debug|info|warn|error (default: info)

# База данных ордеров (SQLite). По умолчанию: data/cryptosignal.db относительно cwd.
# DATABASE_PATH=./data/cryptosignal.db

# Пароль админ-панели (по умолчанию из ТЗ). Для продакшена задайте свой.
# ADMIN_PASSWORD=your_secure_password
